apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mc-pms.fullname" . }}-client
  labels:
    {{- include "mc-pms.componentLabels" (dict "root" . "component" "client") | nindent 4 }}
spec:
  {{- if not .Values.client.autoscaling.enabled }}
  replicas: {{ .Values.client.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "mc-pms.componentSelectorLabels" (dict "root" . "component" "client") | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/http-config: {{ include "mc-pms.httpConfigMap" (dict "component" "client" "prefix" "HTTP_" "config" .Values.client "root" .) | sha256sum }}
        checksum/db-config: {{ include "mc-pms.dbConfig" (dict "component" "client" "prefix" "DB_" "root" .) | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "mc-pms.componentLabels" (dict "root" . "component" "client") | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "mc-pms.serviceAccountName" . }}

      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      containers:
      - name: client
        image: {{ include "mc-pms.image" (dict "image" .Values.client.image "chart" .Chart) }}
        imagePullPolicy: {{ .Values.client.image.pullPolicy }}

        {{- with .Values.securityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        ports:
        - name: http
          containerPort: {{ .Values.client.http.port | default 8080 }}
          protocol: TCP

        envFrom:
        # Load HTTP configuration from ConfigMap (with HTTP_ prefix)
        - configMapRef:
            name: {{ include "mc-pms.httpConfigMapName" (dict "root" . "component" "client") }}
        # Load Database configuration from ConfigMap (with DB_ prefix)
        - configMapRef:
            name: {{ include "mc-pms.dbConfigMapName" (dict "root" . "component" "client") }}

        env:
        # Map database credentials from Secret (with DB_ prefix)
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: {{ include "mc-pms.database.secretName" . }}
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "mc-pms.database.secretName" . }}
              key: password
        {{- with .Values.client.env }}
        {{- toYaml . | nindent 8 }}
        {{- end }}

        {{- with .Values.client.livenessProbe }}
        livenessProbe:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        {{- with .Values.client.readinessProbe }}
        readinessProbe:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        resources:
          {{- toYaml .Values.client.resources | nindent 10 }}

        {{- with .Values.client.volumeMounts }}
        volumeMounts:
          {{- toYaml . | nindent 8 }}
        {{- end }}

      {{- with .Values.client.volumes }}
      volumes:
        {{- toYaml . | nindent 6 }}
      {{- end }}

      {{- with .Values.client.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.client.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.client.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
