{{- include "mc-pms.config.http" (dict
  "component" "client"
  "prefix" "HTTP_"
  "config" .Values.client
  "root" $
) }}
{{- include "mc-pms.config.db" (dict
  "component" "client"
  "prefix" "DB_"
  "config" .Values.client
  "root" $
) }}
