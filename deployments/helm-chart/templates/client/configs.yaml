{{- include "mc-pms.httpConfigMap" (dict
  "component" "client"
  "prefix" "HTTP_"
  "config" .Values.client
  "root" .
) }}
{{- include "mc-pms.dbConfig" (dict
  "component" "client"
  "prefix" "DB_"
  "root" .
) }}
