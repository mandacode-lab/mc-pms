{{- include "lib.config.http" (dict
  "component" "admin"
  "prefix" "HTTP_"
  "config" .Values
  "root" $
) }}
{{- include "lib.config.db" (dict
  "component" "admin"
  "prefix" "DB_"
  "config" .Values
  "root" $
) }}
