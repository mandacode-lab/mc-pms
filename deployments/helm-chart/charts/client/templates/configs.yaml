{{- include "pms-lib.config.http" (dict
  "component" "client"
  "prefix" "HTTP_"
  "config" .Values
  "root" $
) }}
{{- include "pms-lib.config.db" (dict
  "component" "client"
  "prefix" "DB_"
  "config" .Values
  "root" $
) }}
